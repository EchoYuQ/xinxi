<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lzy.heartset">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_main"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.lzy.heartset.activity.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name="com.lzy.heartset.activity.MeasureActivity">

        </activity>

        <activity android:name=".activity.SaveDataActivity">

        </activity>

        <activity android:name="com.lzy.heartset.activity.LoginActivity">

        </activity>
        <activity android:name="com.lzy.heartset.activity.RegisterActivity">

        </activity>
        <activity android:name="com.lzy.heartset.activity.ResultActivity">

        </activity>
        <activity android:name=".activity.HistoryActivity">

        </activity>
        <activity android:name=".activity.WebActivity">

        </activity>

        <activity android:name=".activity.LogoActivity">

        </activity>

        <activity android:name=".activity.MyInformationActivity">

        </activity>

        <activity android:name=".activity.ResetPwdActivity">

        </activity>
    </application>

</manifest>
